services:

type: web
name: telegram-binance-bot
env: python
buildCommand: "pip install -r requirements.txt"
startCommand: "python telegram_binance_bot.py"
envVars:

key: TELEGRAM_BOT_TOKE
sync: false

key: BINANCE_API_KEY
sync: false

key: BINANCE_API_SECRET
sync: false
